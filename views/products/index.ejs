<h1>Today's Deals</h1>

<ul>
  <% products.forEach ( product => { %>
    <li>
        <img src="<%= product.img %>" alt="<%= product.name %>"/>
        <h2><%= product.name %></h2>
        <p><%= product.price %></p>
    </li>
  <% }) %>
</ul>