<!-- navbar missing!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! -->

<img src="<%= product.img %>" alt="<%= product.name %>" />

<h1><%= product.name %></h1>

<p><%= product.price %></p>

<p>Description: <%= product.description %></p>

<!-- go back to quantity how it will increase or decrease or replace it with button!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! -->
<p>Quantity Available: <%= product.quantity %></p>

<button type="submit">Add to Cart</button>

//  If user owns the product show edit and delete options

<% if (product.seller._id.equals(user._id)) { %>

<a href="/products/<%= product._id %>/edit">Edit Product</a>

<form method="POST" action="/products/<%= product._id %>?_method=DELETE">
  <button type="submit">Delete Product</button>
</form>  

<% } %>

